{
  "name": "shared-use-web-app",
  "version": "1.0.0",
  "description": "The web version of the shared use app.",
  "scripts": {
    "start": "http-server",
    "lint": "standard --fix public/js/*.js && standard --fix public/js/controllers/*.js"
  },
  "devDependencies": {
    "standard": "^9.0.1"
  },
  "standard": {
    "globals": ["angular"]
  }
}
