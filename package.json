{
  "name": "shared-use-web-app",
  "version": "1.0.0",
  "description": "The web version of the shared use app.",
  "main": "index.js",
  "scripts": {
    "lint": "standard --fix public/js/**"
  },
  "devDependencies": {
    "standard": "^9.0.1"
  },
  "standard": {
    "globals": ["angular"]
  }
}
